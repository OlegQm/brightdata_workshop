# LLM Chat DTOs

Pydantic request and response models for internal chat endpoints.

Files:

- `request.py` - incoming chat message and optional session id.
- `response.py` - session clear response.

These are internal API contracts between `backend` and `llm`.
