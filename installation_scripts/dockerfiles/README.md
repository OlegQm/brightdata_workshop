# Data Download Dockerfiles

Docker images used only by `docker-compose.data-download.yml`.

Files:

- `Dockerfile.czsk-tiles` - environment for downloading/building CZ/SK PMTiles.
- `Dockerfile.photon-pbf` - environment for downloading Photon/OSM PBF data.

Application services do not use these images at runtime.
