# Scripts

Reserved directory for local project utilities.

Use this for host-side helper scripts that do not belong to a specific service.
Data download scripts live in `installation_scripts/sh/` because they are run
inside purpose-built containers.
