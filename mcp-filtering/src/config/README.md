# MCP Filtering Config

Typed runtime configuration for the MCP filtering service.

`config.py` reads nested `MCP_FILTERING__...` variables and the compose
compatibility variable `DATABASE_URL`.

Default bind address is `0.0.0.0:8001`.
