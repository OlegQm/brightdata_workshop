# MCP Filtering Models

Small normalization helpers for tool filters.

`filter.py` keeps user/LLM-provided values safe before SQL construction, for
example normalizing text and clamping result limits.
